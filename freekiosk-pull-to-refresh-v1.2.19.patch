diff --git a/src/components/WebViewComponent.tsx b/src/components/WebViewComponent.tsx
index fcad9df..290b5b8 100644
--- a/src/components/WebViewComponent.tsx
+++ b/src/components/WebViewComponent.tsx
@@ -1025,6 +1025,9 @@ const WebViewComponent = forwardRef<WebViewComponentRef, WebViewComponentProps>(
 
         nestedScrollEnabled={true}
 
+        // Pull-to-refresh: swipe down from the top of the page to reload (Android SwipeRefreshLayout)
+        pullToRefreshEnabled={true}
+
         mediaPlaybackRequiresUserAction={false}
         allowsInlineMediaPlayback={true}
 
@@ -1089,6 +1092,17 @@ const WebViewComponent = forwardRef<WebViewComponentRef, WebViewComponentProps>(
           <Text style={styles.blockedToastText}>{blockedUrlMessage}</Text>
         </View>
       )}
+
+      {/* Manual page refresh button - fixed top center. Replaces pull-to-refresh,
+          which is unreliable on non-scrolling / inner-scroll SPA pages. */}
+      <TouchableOpacity
+        style={styles.refreshButton}
+        onPress={() => webViewRef.current?.reload()}
+        activeOpacity={0.6}
+        hitSlop={{ top: 10, bottom: 10, left: 10, right: 10 }}
+      >
+        <Text style={styles.refreshButtonText}>⟳</Text>
+      </TouchableOpacity>
     </View>
   );
 });
@@ -1103,6 +1117,27 @@ const FeatureItem: React.FC<{ icon: string; text: string }> = ({ icon, text }) =
 
 
 const styles = StyleSheet.create({
+  refreshButton: {
+    position: 'absolute',
+    top: 8,
+    left: '50%',
+    transform: [{ translateX: -20 }],
+    width: 40,
+    height: 40,
+    borderRadius: 20,
+    backgroundColor: 'rgba(0, 0, 0, 0.35)',
+    justifyContent: 'center',
+    alignItems: 'center',
+    zIndex: 9999,
+    elevation: 8,
+  },
+  refreshButtonText: {
+    color: '#ffffff',
+    fontSize: 24,
+    lineHeight: 28,
+    fontWeight: '600',
+    marginTop: -2,
+  },
   // WELCOME SCREEN STYLES
   welcomeContainer: {
     flex: 1,
